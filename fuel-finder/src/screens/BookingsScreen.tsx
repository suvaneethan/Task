import { View, Text } from 'react-native';

export default function BookingsScreen() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Bookings (Placeholder)</Text>
        </View>
    );
}
